[userinfo]  # 必须包含此节名
email=email=849489858@qq.com  # 替换为实际邮箱
key=30bffdb13deccd433f6505f2dc699972       # 你的API KEY（即提供的密钥）

[size]  # 可选配置，默认值可保留
size=100  # 单次查询返回结果数量（默认100，最大1000）

[full]  # 可选配置，默认值可保留
full=false  # 是否返回完整数据（默认false）

